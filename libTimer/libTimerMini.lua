TIMERS={}function TIMERS:addTimer(a,b)assert(b~=nil,"func must not be nil")assert(type(b)=="function","func must be a callback function")assert(a~=nil,"interval must not be nil")assert(type(a)=="number","interval must be a number")assert(a%1==0,"interval must be a positive integer")assert(a>0,"interval must be bigger than 0")table.insert(self,{modT=a,run=b})end;function TIMERS:tick(c)assert(c~=nil,"t must not be nil")assert(type(c)=="number","t must be a number")for d=1,#self do local e=self[d]if c%e.modT==0 then e.run()end end end
